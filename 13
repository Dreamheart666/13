#include <stdio.h>
#include <windows.h>
#include<string.h>
#include <math.h>
#include<windows.h>
int main()
{
	int arr[10] = { 0 };
	int i = 0;
	int* p = arr;
	for (i = 0; i < 10; i++)
	{
		*(p + i) = i;
		printf("%d ===== %d\n", *(p + i), arr[i]);
	}
	return 0;
}
